# https://github.com/hitblast
# Documentation reference: https://nikitabobko.github.io/AeroSpace/guide

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
#                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
#                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
#                   sectionSign (ISO keyboards only, european keyboards only)
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

config-version = 2

after-login-command = []
after-startup-command = []

start-at-login = true

enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

accordion-padding = 25

default-root-container-layout = 'tiles'
default-root-container-orientation = 'horizontal'

on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
automatically-unhide-macos-hidden-apps = false

on-window-detected = [
    {
        if = '''
        test %{app-bundle-id} = dev.zed.Zed \
            || test %{app-bundle-id} = app.liten.Gram \
            || test %{app-bundle-id} = com.mitchellh.ghostty \
            || test %{app-bundle-id} = dev.kdrag0n.MacVirt \
            || test %{app-name} ~= xcode \
            || test %{app-bundle-id} = org.keepassxc.keepassxc
        ''',
        run = 'move-node-to-workspace U',
    },
    {
        if = 'test %{app-bundle-id} = com.apple.Notes',
        run = 'move-node-to-workspace N',
    },
    {
        if = '''
        test %{app-bundle-id} = com.apple.Safari \
            || test %{app-bundle-id} = net.imput.helium
        ''',
        run = 'move-node-to-workspace I',
    },
    {
        if = 'test %{app-name} ~= aseprite',
        run = 'move-node-to-workspace O',
    },
    {
        if = 'test %{app-bundle-id} = dev.vencord.vesktop',
        run = 'move-node-to-workspace P',
    },
    {
        if = '''
        test %{app-bundle-id} = com.colliderli.iina \
            || test %{app-bundle-id} = com.spotify.client
        ''',
        run = 'move-node-to-workspace M',
    },
    {
        if = '''
        test %{app-name} ~= steam \
            || test %{app-bundle-id} = io.github.Mrmayman.QuantumLauncher
        ''',
        run = 'move-node-to-workspace G',
    },
]

[key-mapping]
    preset = 'qwerty'

[gaps]
    inner.horizontal = 8
    inner.vertical = 8
    outer.left = 0
    outer.bottom = 0
    outer.top = 0
    outer.right = 0

[mode.main.binding]
    alt-slash = 'layout tiles horizontal vertical'
    alt-comma = 'layout accordion horizontal vertical'

    alt-h = 'focus left'
    alt-j = 'focus down'
    alt-k = 'focus up'
    alt-l = 'focus right'

    alt-f = 'fullscreen'

    alt-shift-h = 'move left'
    alt-shift-j = 'move down'
    alt-shift-k = 'move up'
    alt-shift-l = 'move right'

    alt-enter = 'exec-and-forget open -a /Applications/Ghostty.app'
    alt-backslash = 'exec-and-forget open -a Launchpad'

    alt-shift-minus = 'resize smart -50'
    alt-shift-equal = 'resize smart +50'

    alt-u = 'workspace U'
    alt-i = 'workspace I'
    alt-o = 'workspace O'
    alt-p = 'workspace P'
    alt-n = 'workspace N'
    alt-m = 'workspace M'
    alt-g = 'workspace G'

    alt-shift-u = 'move-node-to-workspace U'
    alt-shift-i = 'move-node-to-workspace I'
    alt-shift-o = 'move-node-to-workspace O'
    alt-shift-p = 'move-node-to-workspace P'
    alt-shift-n = 'move-node-to-workspace N'
    alt-shift-m = 'move-node-to-workspace M'
    alt-shift-g = 'move-node-to-workspace G'

    alt-leftSquareBracket = 'workspace-back-and-forth'
    alt-shift-semicolon = 'mode service'

[mode.service.binding]
    esc = ['flatten-workspace-tree', 'mode main']
    r = ['reload-config', 'mode main']
    f = [
        'layout floating tiling',
        'mode main',
    ]

    alt-shift-h = ['join-with left', 'mode main']
    alt-shift-j = ['join-with down', 'mode main']
    alt-shift-k = ['join-with up', 'mode main']
    alt-shift-l = ['join-with right', 'mode main']
